apiVersion: v1
kind: Service
metadata:
  name: {{ include "spring-cubix.fullname" . }}
  labels:
    {{- include "spring-cubix.labels" . | nindent 4 }}
spec:
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    - port: 9080
      targetPort: management
      protocol: TCP
      name: management
  selector:
    {{- include "spring-cubix.selectorLabels" . | nindent 4 }}
